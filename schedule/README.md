# Schedule Video Generator

Makes a schedule video. 

## Usage

Just edit schedule.js to match the schedule you'd like to use, put corresponding images into the videos/ directory,
and run the recorder with `node .`.

It will output the schedule as `schedule.mp4`, ready to share.